#pragma once

// Copy this file to Credentials.h and fill in your WiFi credentials.
// Credentials.h is excluded from version control via .gitignore.

#define WIFI_SSID       "your-ssid"
#define WIFI_PASSWORD   "your-password"
